# Specification Quality Checklist: Read an AU Small Finance Bank (Savings/Current) Statement On-Device — the Fourth and Final Balance-Ledger Reference Reader (AU Config on the Existing Ledger Base, One Statement Template, Dash-Marked Empty Debit/Credit Columns, Delta-Derived Direction Despite UPI/DR·UPI/CR Counterparty Text)

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-07-17
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] No implementation details (languages, frameworks, APIs)
- [x] Focused on user value and business needs
- [x] Written for non-technical stakeholders
- [x] All mandatory sections completed

## Requirement Completeness

- [x] No [NEEDS CLARIFICATION] markers remain
- [x] Requirements are testable and unambiguous
- [x] Success criteria are measurable
- [x] Success criteria are technology-agnostic (no implementation details)
- [x] All acceptance scenarios are defined
- [x] Edge cases are identified
- [x] Scope is clearly bounded
- [x] Dependencies and assumptions identified

## Feature Readiness

- [x] All functional requirements have clear acceptance criteria
- [x] User scenarios cover primary flows
- [x] Feature meets measurable outcomes defined in Success Criteria
- [x] No implementation details leak into specification

## Notes

- **Validation result**: All items pass; the specification is ready for `/speckit.clarify` or `/speckit.plan`. Zero `[NEEDS CLARIFICATION]` markers were needed — the feature description carried the complete reference ground truth (every row's date/amount/direction/stitched-description/balance/delta/empty-serial/direction-source, the printed opening/closing balances, the billing period, the account last-4, the gate markers, the single anchor shape, the opening/closing/period/account patterns, and the RECONCILED balance-chain result), which is additionally persisted as a captured JSON ground-truth artifact (one fixture, reconciled). That, plus the constitution, the ICICI reference slice (`specs/007-bank-account-ledger-reader/`), the HDFC drop-in (`specs/008-hdfc-bank-ledger-reader/`), the Federal drop-in (`specs/009-federal-bank-ledger-reader/`), and the web-engine source of truth (`au_bank.py` + the shared balance-ledger reader / balance-chain / common), made the context complete, so open details were resolved as documented Assumptions (informed defaults deferred to `/speckit.plan`) rather than questions.
- **Ground-truth cross-check (automated)**: Every reference value asserted in the spec was verified byte-for-byte against the captured ground-truth JSON — both row descriptions (incl. row 1's folded `UPI/DR/000000000001/EXAMPLE ABC0000000001ref` above-anchor line and `MERCHANT/UTIB/0000/UPI AU` continuation, and row 2's folded `UPI/CR/000000000002/EXAMPLE XYZ0000000002ref` above-anchor line, `SALARY/UTIB/0000/UPI AU` continuation, and trailing footer `1800 1200 1200 www.aubank.in customercare@aubank.in`), empty serials, amounts (`5000.00`/`10000.00`), balances (`6570.79`/`16570.79`), deltas (`−5000.00`/`+10000.00`), direction sources (`opening_balance`/`balance_delta`), period (`2026-03-01`→`2026-05-31`), last-4 (`0042`), and the printed opening/closing balances (`11570.79`/`16570.79`). Negative deltas are rendered with the typographic minus `−` (consistent with the landed HDFC 008 and Federal 009 specs); the fixtures themselves store ASCII `-`.
- **Verified against the repo — this is a config-on-an-existing-base slice with ZERO new shared code**: Confirmed directly. The reusable base already exists (`core/crates/kaname-core/src/statement/ledger_reader.rs`): its `LedgerReaderConfig` trait recognizes a transaction against an **ordered list** of anchor patterns (`anchor_res() -> Vec<&Regex>`, "tried in order (first match wins)", `ledger_reader.rs:60`) — so AU's single pattern is pure configuration; its `anchor_amount` resolves a two-column `withdrawal`/`deposit` pair, returning the **non-zero / non-empty** side (`ledger_reader.rs:238-257`); and its `loose_amount` returns `None` for a token that `Decimal::from_str_exact` rejects (`ledger_reader.rs:262-268`) — so AU's dash (`-`) empty column yields no value and the non-dash column becomes the amount, **with no base change** (the key AU mechanic). Its stitcher skips only `is_balance_line` matches (`ledger_reader.rs:302`, `395-400`) — so the trailing footer line (which matches no anchor and no opening/closing pattern) is folded into the last row's narration exactly as the ground truth shows, and the `Closing Balance(₹)` line is skipped once a closing pattern is supplied.
- **Verified — dash handling trace**: For row 1 (`… 5,000.00 - 6,570.79`), `withdrawal`=`5,000.00`→`5000.00` (non-zero) ⇒ amount `5000.00`; balance fell `11570.79`→`6570.79` ⇒ **debit**. For row 2 (`… - 10,000.00 16,570.79`), `withdrawal`=`-`→`loose_amount("-")`=`None`, `deposit`=`10,000.00`→`10000.00` (non-zero) ⇒ amount `10000.00`; balance rose `6570.79`→`16570.79` ⇒ **credit**. Both amounts equal `|delta|` ⇒ zero suspects ⇒ RECONCILED. This matches the ground truth exactly (US3, FR-004/FR-005, SC-004).
- **Verified — the shared account-tail helper already exists (no new shared helper)**: `core/crates/kaname-core/src/statement/common.rs:177-188` already provides `account_tail_last4(text, primary)` (per-bank primary regex → trailing four, else the longest standalone ≥9-digit run → trailing four), used by `icici_bank.rs`, `hdfc_bank.rs`, and `federal_bank.rs`. AU reuses it with its own primary regex (`Account Number` + optional `X*` mask + 6+ digits), matching the full number `1234567890120042`→`0042`. This slice adds **no** new shared code at all (FR-023, US8, SC-008).
- **Verified — the shared date parser already carries the AU date format**: `common.rs:30` already lists `%d %b %Y` ("21 Apr 2026"), which is exactly AU's `DD Mon YYYY` (`01 Mar 2026`), so no date-parser change is needed — supporting FR-019/FR-025 and the "no new shared engine internals / no new date format" boundary.
- **Verified — printed closing balance is the last row's running balance, not the header figure**: `ledger_reader.rs:195` sets `printed_closing_balance = Some(anchors[last].balance)` unconditionally; the base's `extract_balance` is only ever called with the **opening** pattern (`ledger_reader.rs:134`), never a closing pattern. So AU's `printed_closing_balance` = row 2's `16570.79`, and the header `Closing Balance(₹) : 223.34` is **never** consulted for that value — a `closing_balance_re` only feeds `is_balance_line` for narration-skip (`ledger_reader.rs:395-400`). This confirms FR-012/FR-013, US4 scenario 4, and SC-007 exactly, and matches the ground truth (`printed_closing_balance = "16570.79"`).
- **Verified — the two-column amount groups are named `withdrawal`/`deposit` in the base**: `anchor_amount` reads `caps.name("withdrawal")` and `caps.name("deposit")` (`ledger_reader.rs:242-245`), so AU's anchor must name its Debit/Credit columns `withdrawal`/`deposit`. The spec's illustrative regex (Assumptions) does this and notes "the base's named `withdrawal`/`deposit` groups carry AU's Debit/Credit columns"; the Debit→`withdrawal` (fall⇒debit) / Credit→`deposit` (rise⇒credit) mapping is consistent with the delta-derived direction.
- **Verified — the reader-config model (single template)**: `icici_bank.rs` is the single-anchor precedent (`anchor_res()` returns `vec![&ANCHOR_RE]`), while `federal_bank.rs`/`hdfc_bank.rs` return two. AU mirrors the ICICI single-anchor shape plus an opening pattern, a closing pattern (for narration-skip), a period pattern, and an account pattern fed to `account_tail_last4`, exactly as `federal_bank.rs:77-105` and `icici_bank.rs:60-100` do. `mod.rs:9-25` shows readers are registered per-issuer modules; the new `au_bank` module placement and the registry wiring are deferred to `/speckit.plan`.
- **Judgment call — the no-marker / UPI/DR·UPI/CR-is-narration rule is behavioural data, not implementation**: The defining AU twist — AU prints **no** per-row Dr/Cr direction marker, and the `UPI/DR`/`UPI/CR` tokens are *inside the narration* (the counterparty's leg) and must **not** drive direction, which is taken from the balance delta — is stated as verifiable behaviour (US2, FR-006/FR-007, SC-003: the debit row's narration contains `UPI/DR` and the credit row's contains `UPI/CR`, yet directions follow the deltas debit-then-credit). This describes the statement's own printed format and the engine's proven behaviour, not a prescribed technology.
- **Judgment call — the dash-marked empty column is behavioural data**: The single anchor shape (two `DD Mon YYYY` dates, then desc, then a Debit column and a Credit column each of which is **either** a money token **or** a dash `-`, then the running Balance), the non-dash-side-is-the-amount rule, the direction-from-delta rule, and the printed-amount-as-independent-check describe the *statement's own printed format and the engine's proven behaviour* the reader must reproduce — behavioural inputs/outcomes, exactly like the landed ICICI/HDFC/Federal ledger specs. The requirements prescribe no engine technology (no raw regex/module/type appears in the FRs); the concrete regex realization, module placement, UniFFI surface names, and fixture path are deferred to `/speckit.plan` (Assumptions). The illustrative regexes in the Assumptions section are explicitly labelled "illustratively … realized in `/speckit.plan`", mirroring the Federal spec's accepted treatment.
- **Judgment call — the narration-stitching quirk is faithful parity data, not a bug**: The requirement that the `UPI/…` reference line printed above an anchor is folded into that row's narration and that the trailing footer line is folded into the last row's narration — reproduced byte-for-byte, "not cleaned up" (US5, FR-015) — is behavioural ground truth ported from the web engine and pinned by the golden fixture. It is stated as verifiable expected strings rather than prescribed code. Excluding the column-header/`Opening Balance(₹)`/`Closing Balance(₹)` lines from the transaction list (FR-016: exactly 2 rows) is likewise a verifiable outcome.
- **Judgment call — `direction_source`, RECONCILED/NEEDS_REVIEW, and suspect vs errored are reused domain vocabulary**: The `direction_source` values (`opening_balance` for row 1; `balance_delta` for row 2), the balance-chain statuses (RECONCILED / NEEDS_REVIEW), and the suspect-vs-errored distinction are inherited unchanged from the base/balance-chain and verified against the golden vector — behavioural acceptance labels, not prescribed code. `NEEDS_REVIEW` (with an underscore) is a status literal, not a `[NEEDS CLARIFICATION]` marker.
- **Judgment call — the account last-4 extractor and privacy**: The last-4 is the trailing four digits of the printed account number via AU's own primary pattern (`Account Number` + optional masked `X*` prefix + 6+ digits, matching `1234567890120042`→`0042`), else the longest ≥9-digit run, keeping only the trailing four (the full number is never logged/persisted). This is an accuracy + privacy outcome (FR-020, US6, SC-007), stated as a testable outcome rather than prescribed code.
- **Judgment call — the AU gate and the no-credit-card-reader distinction**: AU's `claims` gate requires the `AU` bank code + `aubank.in` + any of (`Savings Account`, `Current Account`); a credit-card statement lacking a Savings/Current marker is rejected (US7, FR-001/FR-002, SC-006). Unlike ICICI/HDFC/Federal (which share an issuer code with a coexisting credit-card reader), AU has **no** credit-card reader in this client, so the AU bank reader is the sole reader under the `AU` code — this is recorded as a factual distinction, not a `[NEEDS CLARIFICATION]`. `claims_ledger` lower-cases both sides (`ledger_reader.rs:97-109`), so all markers match case-insensitively.
- **Judgment call — behavioural characterization data**: The spec quotes the one synthetic AU statement's expected output (rows 2026-03-01/5000.00/debit and 2026-03-02/10000.00/credit with empty serials), the balances (6570.79, 16570.79) and deltas (−5000.00, +10000.00), the printed opening/closing balances (11570.79/16570.79), the billing period, the account last-4, and the RECONCILED balance-chain result. These are the constitution's golden-fixture parity vectors (Principle V) — behavioural acceptance data, not implementation details — and all data is synthetic/redacted.
- **Judgment call — technology proper nouns**: The only technology names in the document are intentional and confined to non-prescriptive locations: the verbatim **Input** line; the **Assumptions / Dependencies / Out of Scope** sections (recording locked decisions such as UniFFI, native PDFKit text/geometry extraction, Apache-2.0, decimal money, the reused base/balance-chain/account-tail-helper/harness/gate, and deferred scope like SQLCipher, each noted as belonging to `/speckit.plan`); and constitution-mandated gate names (privacy-egress test, iOS Local Verification Gate, CI). SC-011's "reachable over the UniFFI bridge to Swift" mirrors the accepted phrasing in the landed ICICI/HDFC/Federal bank-account success criteria.
- **Judgment call — accessibility & privacy vocabulary**: References to Human Interface Guidelines, Dynamic Type, Dark Mode, VoiceOver, "zero network I/O", "no new networking dependency", and "no telemetry" are treated as user-facing outcomes mandated by the constitution's Privacy and Native Experience principles, not framework/API implementation choices.
- **Scope-boundary note (recorded for the reviewer, not a blocker)**: This spec records AU as the **fourth and final** bank on the balance-ledger base, and does not modify the landed ICICI/HDFC/Federal ledger readers. This classification does not affect this slice's scope, so it is surfaced here rather than raised as a `[NEEDS CLARIFICATION]` marker.
- Items marked incomplete would require spec updates before `/speckit.clarify` or `/speckit.plan`; none are incomplete.
